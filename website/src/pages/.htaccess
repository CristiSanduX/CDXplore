Options -Indexes
RewriteEngine On

# daca e fisier sau folder real, lasa-l
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# altfel, trimite tot la SPA
RewriteRule ^ index.html [L]
